/**
 * Array:
 *      which can store one or more values
 *      technically, is an object
 *      represent by []
 * 
 * functions:
 *      Array-Concept-1.js
 *              length (property)
 *              []
 *      Array-Concept-2.js
 *              toString()
 *              join()
 *              pop()
 *              shift()
 *              push()
 *              unshift()
 *              splice()
 *              concat()
 *              includes()
 *              indexOf()
 *              lastIndexOf()
 *              isArray()
 *              reverse()
 *              fill()
 * 
 */
/**
 * isArray()
 */

let ab = '10';    // string

/** 
 * is value in 'ab' variable an array or not?
 */

console.log(Array.isArray(ab));

